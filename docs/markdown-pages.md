# Markdown Pages

Add `.md` files in these directories and render using `@comark/nuxt`:

- `app/pages/policy/`
- `app/pages/changelog/` except `index.vue`

Only create `.md` pages for documentations.
